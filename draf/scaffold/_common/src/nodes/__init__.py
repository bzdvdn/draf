"""Nodes package for {{PROJECT_NAME}}.

Context builders are re-exported from :mod:`draf.node.context` here so
``src.graphs.build`` reads the same as a hand-written app; the
:class:`draf.node.supervisor.Supervisor` decider also comes from core and
is added with :meth:`draf.flow.Flow.supervisor`.
"""
