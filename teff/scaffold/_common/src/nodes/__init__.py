"""Nodes package for {{PROJECT_NAME}}.

Context builders are re-exported from :mod:`teff.node.context` here so
``src.graphs.build`` reads the same as a hand-written app; the
:class:`teff.node.supervisor.Supervisor` decider also comes from core and
is added with :meth:`teff.flow.Flow.supervisor`.
"""
