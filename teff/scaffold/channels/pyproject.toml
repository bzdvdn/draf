[project]
name = "{{project_slug}}"
version = "0.1.0"
description = "{{PROJECT_NAME}} — YAML-first teff channels app"
requires-python = ">=3.11"
dependencies = [
    "teff[channels]>=0.1.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
