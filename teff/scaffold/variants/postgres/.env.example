# {{PROJECT_NAME}} — example environment for the `postgres` variant.
# Copy to `.env` and adjust: sessions (and RAG vectors, with the `rag`
# variant) live in Postgres instead of local JSON/SQLite files.
DRAF_DATABASE_URL=postgresql://{{project_slug}}:{{project_slug}}@localhost:5432/{{project_slug}}
